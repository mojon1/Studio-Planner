# テストが確かめていること

`test/run.mjs` の全ブロックが見ているものの一覧（`CLAUDE.md` から移した）。ブロックを足したらここにも足す。

`test/out/` にスクリーンショットが出る。確認しているのは、JS エラーの有無、
共有リンクの往復と復元、旧リンクの移行、GLB 取り込みの寸法、センサー換算値、
回転リングのドラッグ、閲覧モード、数値の直接入力、寸法ラベルが重ならないこと、
PDF の用紙サイズとページ数、用紙の再構図とファインダーが動かないこと、
保存ダイアログの 3 経路（OS のダイアログ／閉じたら何もしない／自前の名前入力。
プロジェクトファイルを外したので、同じ道を通る画像で見ている）、
**プロジェクト保存／読込がパネルに無いこと**、
**リンクの下に「※外部3Dデータは含まれません。」が出ていること**、
**共有タブの説明がその 1 行と HTML書き出しの一言の 2 つだけであること**、
**取り込みのボタンが「外部3Dデータ」で、説明が 3 行だけであること**、
人物モデルが身長どおりに縮尺されること、
身長を縮めても種別とモデルが変わらないこと、+ の一覧に**カジュアルだけ**がサムネイル付きで
並び、選んだ人がその身長で置かれること、**設定パネルの「服装」でスーツに着替えても
地域・種別・身長が変わらないこと**、**服装がポーズの上にあること**、
**子どもには着替えが出ないこと**、ポーズ 8 種がパネルの最下部に畳まずに並び、
座る・寝るが効くこと、
当たり判定の 3 段（ジオメトリ／リングの内側／リング）、右クリックのメニュー、
複製とロックとゴミ箱、辺のドラッグでの大きさ変更、
カメラ小窓のドラッグでパン・チルトが効いて絵が描き直されること、
取り消しとやり直し（ボタンと Ctrl+Z / Ctrl+Shift+Z）、カットの追加・切替・改名と
リンクへの往復、全カット PDF がカットごとに 1 枚になること、
縦持ちで右上の情報表示がビューボタンに重ならないこと、
**QR が参照実装（npm の `qrcode`）と 1 モジュールずつ一致し、`jsQR` で読めること**、
**Service Worker が入って、通信を切っても人物モデルごと開くこと**、
**3DGS のスキャン**（手で組んだ 8 個の PLY）が実寸で入り、クリックでは選ばれず、
スタジオを消せて、**消したら撮影範囲の四角錐が壁で切れずに画面の大きさで描かれること**、
**組み直した直後の並べ直しでスキャンが原点に飛ばないこと、他の物の目のマークで 3DGS の group が
作り直されないこと**、
共有リンクには置き方だけが乗ること、
**スタジオを消してもグリッドが残り、残ったものに合わせて縮むこと**、
**目のマークで消したら、視点を動かさなくてもその場でスプラットが消えること**、
**3DGS に寸法が出ないこと**（パネルの「寸法表示」も、上面のラベルも）、
**向きが高さ調整の上にあること**、
**トリミング**（「トリミング」でモードに入って 6 面のつまみと箱と 7 本のスライダーが出て、向かい合う面が追い越さず、
「確定」で箱を残したまま消え、もう一度押せば同じ箱で戻り、「トリミングのリセット」で外形いっぱいに戻り、
つまみが画面の短辺の 3% で描かれること）、
**3DGS が鏡に映ること**（色付きのスキャンを置き、鏡の面を読んでから目のマークで
消し、同じところが変わるかで見る）、
**ライト**（`+` のボタンが 1 つだけで、置くとスポットがスタンドに乗ること。
パネルに 5 つの型と 3 つの載せ方と「簡易表示です／set.a.light 3D」の一言が出ること。
型を変えると広がりとボケが入れ替わり、**それでも three のライトは作り直されない**こと。
**チューブと提灯が全方位で、チルトも広がりもボケも出ないこと**（提灯は向きも）、
**チューブが 1 点ではなく棒に沿った 3 点であること**、
**LED パネルとソフトボックスに広がり・ボケのつまみが出ないこと**、
**器材の筐体が影を落とさず、光源が発光面まで出ていること**（スタンドは落とす）、
チルトが ±90 まで開いて狙いがそのとおりに向くこと、強さで届く距離と効きが変わること、
ゼラが届くこと、**パースではゼラを替えると床の色が動き、上面図では 1 も動かない**こと、
目のマークで消えること、**6 灯置いても影は 4 灯まで**で全方位は落とさないこと、
共有リンクに型・載せ方・広がり・ボケ・強さ・チルト・色が全部乗ること、
**古いリンクのライトがスポットとして開く**こと、GLB に入ること、消したら
three のライトも消えること）、
**環境光のつまみ**（ライト無しで 50、置くと 25 に自動で下がり、手で入れた値は 2 灯目では
動かず、全部消すと 50 に戻ること。0 で絵の地明かりが 0 になり、図面は平らなままなこと。
共有リンクに乗ること）、
**街並みのような大きな 3DGS**（何も入れていないうちは移動範囲が今までどおり
部屋の中で、グリッドも 1 m 刻みのままであること。100 倍にしたら移動範囲・上面図の
画角・カメラの far がそこまで広がり、`setPos()` で 90 m 先へ本当に動かせて、
グリッドの刻みが上がって右上にその旨が出ること）、
**拡大縮小が 1/100〜100 倍の対数で、つまみの真ん中がちょうど 1 倍であること**、
**コロフォンに寺村さんのサイト URL が入っていること**、**共有タブのいちばん下が紹介動画（YouTube）の
リンクであること**、
**3DGS を出したままカメラの小窓を開いても並べ替えが落ち着くこと**（何も触らずに
描画が 1 枚も増えないこと、並べ替えの途中で止まっていないこと、**並べたのが
小窓ではなく本体のビューのカメラ**であること、視点を回せば新しい視点で並べ直して
また止まること）、
**書き出しボタンが「HTML書き出し」で、iPhone の UA では `accept` が外れること**、
**共有タブに見出しが 1 つも無く、7 つのボタンが名前だけで並び、どれにも
`primary` が付いておらず、見るだけリンクのボタンと URL の欄が出ていないこと**、
**QR に「見るだけ」の切り替えが無く、古い `m=v` のリンクは今までどおり
読むだけで開くこと**、
**取り込みの上限が 3D モデル 120MB / 3DGS 240MB で、画面に出す文言と
数字が一致していること**、
**携帯で `+` から置いても、パネルの開き／畳みが置く前のまま変わらないこと**
（畳んだままでも選択とタブ切り替えは効いていて、歯車で開けばその物の設定が出る）、
**画面が文字選択にならないこと**（`body` は `none`、入力欄だけ `text`、
iOS 向けの `-webkit-touch-callout:none` が入っていること）、
**一覧の行を指で長押しすると右クリックと同じメニューが出ること**
（オブジェクトもカットも。指を滑らせたら取り消し、マウスでは出ない、
出た直後は指が突き抜けない）、
**小窓がカメラの比率そのものであること**（16:9 で開き、9:16 にすると縦長になって画面に収まり、
4:3 でも合い、つまみで引いても比率が変わらないこと）、
**寸法表示が人物でもカメラでも最後にあって「カメラ距離」と書いてあること、カメラの頭の上の
つまみで高さが上がってファインダーが追い、上面図では出ないこと、椅子の上では立つポーズが座面に
立ち、椅子のポーズならお尻の底が座面に吸着し、座面を上げれば一緒に上がり、床に座る・寝るは
天面に置くだけで、写真ポーズも同じ判定で、椅子をどけると床に戻ること**、
**定規**（＋の一覧にサムネイル付きで居て、置くと始点のトーストが出て、床を 2 回タップすると
その 2 点が始点・終点になり、長さのラベルが出て、端のつまみでその端だけ動き、箱の天面をタップすると
その高さに乗り、GLB に出ず、リンクに乗り、3DGS の上をタップすると床より上に乗ること。
**向きのつまみもマニピュレータも無く、本体をドラッグしても動かず、「起点をカメラにする」で始点が
レンズに来て、視点の外側の薄い壁を通したタップが壁の裏ではなく床に落ちること**）、
**3DGS の軸と傾き**（傾きと ±50 m の高さと「中心のオフセット」の 3 本のつまみが出て、選んでいるあいだ
軸が画面の短辺の 12% で描かれて視点を引いても同じ大きさで、X に 30° 傾けると軸まわりに振れ、続けて 90° 回すとワールド Y まわりに回り、
**オフセットを動かすとスキャンがそのぶん動いて軸・位置・高さ調整は動かず、10 目盛り引いても
並べ直しは離したときの 1 回**、「傾きを戻す」が無くつまみのリセットで両方 0 にすると yaw だけ残り、
どのつまみでも右クリックで「リセット」のメニューが出て既定に戻り、傾けたまま切り取りのつまみが 6 つ出て、
向きとオフセットがリンクに乗ること。**「軸を操作」が 3DGS を選ぶと出て、オンなら十字のドラッグで軸だけ動いて
スキャンは残り、X の棒をドラッグするとその軸に沿ってスキャンが動くこと**）、
**トリミングの箱のマニピュレータ**（トリミングのモードで箱の中心に青いリングが出て、十字で箱ごと動き（大きさは
変わらない）、リングで箱の中心まわりに回り、「箱の回転」スライダーでも中心が動かず、上面図では上下の
つまみが消え、`ry` がリンクに乗り、やめれば消えること。`.sog` がファイルで読めて、**URL の欄が
もう無いこと**）、
**画面比率の手動**（ボタンの最後が「手動」で、押すと 16 と 9 から始まり、2.39:1 を打つと読み値・小窓・
ファインダーが追い、リンクに乗り、0 は弾かれること）、
**角のつまみ**（床鏡が 4 角、箱は天面の 4 角＋真ん中で上面図では 4 つ、角を引くと幅と奥行が変わって
向かいの角が残り、真ん中で高さが上がること。テーブルの「形」で丸にすると奥行が直径に揃い、
縁のつまみで直径が変わり、リンクに乗ること。小窓が自分の canvas に絵を持ってビューボタンより上に
居ること。背景布の R の法線が内向きなこと）、
**英語表示**（`?eng` でタブ・ビュー・一覧・各設定パネル・追加の一覧・カット・共有・QR・
トースト・寸法ラベル・用紙のバーと用紙に日本語が 1 つも残らないこと。`locale:'en-US'` の
ブラウザは `?eng` 無しで英語、`ja-JP` は日本語のまま、`?lang=en` で強制できること）、
**まとめて書き出した 1 枚**が `file://` で開いて、通信を 1 本も出さず、
人物モデルもスキャンも切り取りも復元されること、**置いていない人物 14 体と車が
全部入っていて、その 1 枚の中から新しく人を置けて、`+` に出ないスーツにも
着替えられること**、**その中の共有リンクが `file://` ではなく Web の URL に
なっていること**、
**書き出した GLB** が GLTFLoader で読み返せて、テクスチャとスキンが入っていて、
メートルで出ていて（10×8）、**カメラの向きも画角もアプリと一致する**こと、
**どのオブジェクトにも読める名前が付いていて**（「Node」「Mesh」が無く、
スタジオの面の名前が重複しないこと。GLB の JSON を直接読んで確かめる）、
**三脚は入りカメラ本体は入らない**こと、
**書き出しの前後で画面の visible が 1 つも変わっていないこと**、
隠したものが入らないこと、ボタンが `.glb` の名前で保存に回ること、
**写真ポーズ指定**（ポーズ一覧の最後がカメラの絵のそのボタンで、入力が `image/*` で
あること。アプリ自身が描いた「座る」を写真として読ませると膝が曲がって戻り、
「立つ」では伸びて戻ること。共有リンクを往復すること。座標の無い `'photo'` は
素の姿勢に戻ること。別のポーズを選ぶと座標が捨てられること）。
テストが中を測れるように `window.__sp` を出してある（アプリ側は誰も読まない）。
`node run.mjs` は失敗があると終了コード 1 で落ちる。
**スタジオの角のつまみ**（スタジオを選ぶと 3D で床の 4 角と高さの 5 つ、上面では 4 つ。角を引くと
中心を保って幅と奥行が変わり、読み値とパネルが追い、縮めるとはみ出した人が中へ戻り、高さのつまみで
天井が上がり、数値入力と同じ 40 m / 15 m で止まり、消したら出ないこと）、
**イントレ**（`+` の小道具に居て 1 段で置かれ、1.83 × 1.22 m、1 段 1.835 m・2 段 3.535 m・4 段 6.935 m、
パネルは段数だけで寸法スライダーが無いこと、丸パイプ 24 本と台皿・布板 7 枚で組んであること、
人とカメラが布板の高さに乗ること、段数がリンクを往復すること、規格外の段数が 4 に丸まること、
上面のラベルが幅 × 奥行、側面のラベルが高さを読むこと）。
