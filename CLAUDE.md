# Studio Planner — 作業のためのメモ

スタジオ撮影の準備と打合せのための 3D Web アプリ。本体は `index.html` 一枚。
人物モデルだけは `models/` の外部ファイルで、必要になったときだけ取りに行く。
開発者は寺村太一さん。日本語でやり取りする。

ローカルの作業フォルダは `D:\05_Claude\Studio-Planner`。

## いちばん大事なこと

**実機で確認していないものを「できました」と書かない。** ヘッドレスブラウザで通ったことと、
iPhone や iPad で本当に指で操作できることは別物。「ここまでは測れた／ここからは未確認」と
分けて書く。

## 誰のための道具か

CG スタッフではなく、**PC に不慣れなプロダクションマネージャー、カメラマン、照明チーム、
ディレクター**が現場で 30 秒で答えを得るための道具。この方針で機能を絞ってある。

- 欲しいのは 3D データではなく「一枚の絵」と「一枚の図面」。
- 光量や配光の物理計算はしない。ライトは位置と向きの記号だけ。
- アカウントとログインは作らない。登録の壁が最大の離脱要因になる。
- 機能を足すときは「初見のスタッフが迷わないか」で判断する。

## やってはいけないこと

1. **機種名のリストを持たない。** カメラは毎年増える。センサーは「フォーマット」で持つ。
   規格は数十年変わらないので更新が要らない。同じ理由で、増え続けるものを
   ハードコードしそうになったら一度止まって考える。
2. **`file://` で開いて動作確認しない。** importmap と ES モジュールを使っているので
   必ず HTTP で配信する。`python -m http.server 8000` でよい。
3. **共有リンクに大きいデータを載せない。** URL ハッシュに圧縮 JSON を入れる方式なので、
   3D モデルの実体は絶対に入らない。実体は IndexedDB、リンクには寸法だけ。
4. **描画パスで一時的に隠した物を、当たり判定でそのまま使わない。**
   カメラ小窓の描画でマニピュレータを非表示にしたまま次のタッチ判定に入り、
   リングが永久に掴めなくなったことがある。判定は状態から引く。
5. **PDF を jsPDF などで直接生成しない。** 日本語のために CJK フォントを数 MB 埋め込むことになる。
   印刷用の HTML を mm 単位で組み、ブラウザの印刷機能に渡す方式で通っている。
   `@page` の size で A4 横／縦を切り替える。
6. **寸法ラベルの重なり判定に固定ピクセル値を使わない。** `labelPositions()` は
   1 文字幅と行高を引数で受ける。画面と、印刷用に大きく描いて小さな枠に縮める用紙とでは、
   同じラベルでも画像に対する相対サイズがまるで違う。用紙側は `15, 34` を渡している。
7. **`<label class="f">` の中のボタンで preventDefault を外さない。** ラベルがクリックを
   スライダーへ転送し、フォーカスが移った瞬間に入力欄が blur して消える。
8. **PDF のファインダーを動かせるようにしない。** 上面・側面・パースは用紙の中で
   ドラッグとホイールで再構図できるが、ファインダーだけは固定してある。
   カメラが実際に撮る画角とずれた絵が現場に出回るのが、この用紙で唯一やってはいけない事故。
9. **勝手にダウンロードさせない。ファイルは必ず `saveFile()` を通す。** 3 段構え。
   デスクトップの Chrome / Edge は `showSaveFilePicker()` で OS の「名前を付けて保存」を
   開く。それが塞がれている場所（claude.ai のプレビュー枠は cross-origin で
   `SecurityError`）では自前の名前入力ダイアログを出し、`claude.use('downloads')` か
   `<a download>` に渡す。**プレビュー枠では `<a download>` は無効**で、押しても
   何も起きないので、リンクだけで済ませない。
   `AbortError` はユーザーがダイアログを閉じただけなので、黙って何もしない。
10. **ダウンロードのファイル名が化けても、すぐアプリを疑わない。** UTF-8 ロケールの無い
   環境の Chromium は、非 ASCII のファイル名を丸ごと捨てて `download` にする。
   `test/run.mjs` はブラウザに `LANG` を渡して回避している。
11. **マニピュレータをオブジェクトのサイズに追従させない。** 以前は外接円から
   半径を出していたので、車や大きな背景布を選ぶとリングが図面を飲み込んだ。
   `GIZMO_R` 固定。中央の十字矢印は「掴んで動かす」の意味で、実際に中は移動判定。
12. **人物モデルを `index.html` に埋め込まない。** 4 体入れれば数 MB が全アクセスに
   乗る。使う人だけが取りに行く外部ファイルにしてある。代償として
   **claude.ai のプレビュー枠では人物モデルが出ない**（枠内から外部ファイルを
   取りに行けない）。プレビューでマネキンのままなのは不具合ではない。
   人物モデルの確認は実際のサーバーか `python -m http.server` で。
13. **人物モデルを後から差し替える UI を作らない。** 誰を置くかは `+` ボタンで
   決まる、というのがこのアプリの約束。設定パネルに「見た目」を戻さない。
14. **人の種別を身長から推測しない。** 以前は `height < 1.40` で男性か男の子かを
   決めていたので、身長を縮めた瞬間に別人になり、人物モデルも消えた。種別は
   `it.kind` に持ち、`+` ボタンで置いたときに決まる。以後は変わらない。
15. **スライダー操作で全アイテムを作り直さない。** `rebuildItem()` で 1 個だけ差し替える。
   取り込んだモデルがあると全体再構築は目に見えて重い。

## 置き場所とバージョンの注意

公開先は **https://mojon1.github.io/Studio-Planner/**。`main` に push すれば出る。
人物モデルが本当に出るかはここでしか確認できない（Artifact のプレビュー枠は
外部ファイルを取りに行けない）。

正典は **GitHub の `mojon1/Studio-Planner`（main）** と **公開中の Artifact** の 2 つ。
この 2 つは簡単にずれる。過去に、別セッションが Artifact 側だけを大きく更新していて、
リポジトリ側が UI ごと古いまま、という状態が起きた。

**作業を始める前に必ず Artifact の現物を読んで、リポジトリと差が無いか確かめる。**
差があれば Artifact 側を土台にしてマージし、両方に反映してから次の作業に入る。
Artifact への publish は、公開版より古いものを出そうとすると拒否される。

## ファイル構成

```
index.html            アプリ本体
models/*.glb          人物モデル。人物に切り替えたときだけ取りに行く
ogp.png               リンクを貼ったときのサムネイル。アプリ自身を撮ったもの
.nojekyll             GitHub Pages に Jekyll を通させない
.github/workflows/pages.yml   main への push で Pages に出る
models/thumbs/*.webp  + ボタンの一覧に出す立ち姿。透過。1 枚 4 KB ほど
tools/shrink-glb.mjs  GLB のテクスチャを縮めて GLB を組み直す
tools/make-thumbs.mjs models/*.glb から models/thumbs/*.webp を焼く
tools/make-ogp.mjs    ogp.png を撮り直す
tools/make-poses.mjs  FBX からポーズ（models/poses.json）とサムネイルを作る
models/poses.json     11 ポーズぶんの関節位置。6 KB。11 体で共通
tools/retarget-prototype.mjs  別リグのポーズを Tripo リグへ移す実験。下の「ポーズ」参照
docs/pose-check/      その結果の絵と、元にした FBX
README.md             使い方と公開方法
test/run.mjs          ヘッドレスブラウザでの確認スクリプト
```

### 人物モデルの入れ方

寺村さん本人が権利を持つ Tripo 生成の GLB。素の状態は 4096×4096 のテクスチャで
3 MB 前後あるので、**必ず縮めてから入れる**。1024 で見た目は変わらず 3 分の 1 になる。

`tools/*.mjs` は playwright を `test/node_modules` から引くので、先に
`cd test && npm install`、それから作業フォルダの直下に
`ln -s test/node_modules node_modules`（Windows なら test の中で実行する）。

```
node tools/shrink-glb.mjs 元.glb models/地域-服装-性別.glb 1024 0.86
node tools/make-thumbs.mjs                # models/ 全部のサムネイルを焼き直す
```

登録は `PEOPLE_MODELS` に 1 行。`id` はファイル名（拡張子なし）、`kind` は
`man` / `woman` / `boy` / `girl`、`wear` はサムネイルの下に出る一語、
`label` は読み上げとツールチップ用。**ファイルは身長 1 m・A ポーズで作る**こと。
読み込み後にバウンディングボックスの高さで割って身長スライダーに合わせている。

`+` ボタンの「人」はこの一覧をサムネイルで並べたもの。**顔がラベル**なので、
名前を読ませない。パネルを初めて開いたときに組み立てるので、開かない人は
サムネイルを 1 枚も取りに行かない。人物を後から別人に差し替える UI は無い
（種別と同じく `+` で決まる）。ポーズは下の「ポーズ」参照。

### ポーズ（実装済み）

寺村さんの C4D の FBX（`docs/pose-check/suwaru01.fbx`、椅子に座る姿）を使って、
**別リグのポーズを Tripo の 11 体に移せることを確認した。** 絵は `docs/pose-check/`。

- C4D 側は **Mixamo 系のリグ**（`mocaprig_Hips` / `Spine` / `LeftUpLeg` …、指まである）。
  Tripo 側（`Hip` / `Waist` / `Spine01` / `L_Thigh` …）とは名前も本数も違う。
- **回転をそのまま写すのは無理。** 参照元の素の姿勢（T ポーズ）が手元に無く、
  2 つのリグは初期姿勢の向きも違う。
- 通ったのは**骨の向きを合わせる方式**。親から順に、その骨から子へ向かうワールド方向が
  参照元と一致するように回す。骨の長さもリグの初期姿勢も無関係になる。
  腰だけは左右の脚の位置で軸まわりのひねりも合わせないと向きが決まらない。
- 22 本を対応づけ、17 本を回して、4 体（男女・体型違い）すべてで破綻なく座った。
  メッシュの破れも左右反転も無し。
- **取れないもの**: 骨の軸まわりのひねり（前腕のひねり、手首の向き）。Tripo に指の骨は
  無いので指も無い。打合せ用の縮尺では問題にならない。
- FBXLoader はこの C4D バイナリ FBX をそのまま読めた。
- **Mixamo から書き出した FBX は、姿勢が 1 フレームのアニメーションに入っている。**
  読んだままの骨はバインド姿勢なので、`AnimationMixer` で 0 秒を適用してから
  ワールド位置を読まないと、全員 T ポーズのままになる。C4D 経由の
  `suwaru01.fbx` はアニメーションが無く骨に焼かれていたので、両方に対応する。
- 骨の接頭辞は書き出し元で変わる（`mixamorig` / `mocaprig_`）。`Hips` で終わる骨を
  探して接頭辞を取る。
- 送っていただいた 11 ポーズ（立つ 5・座る 2・寝る 2・歩く・ダンス）は全部通った。
  `docs/pose-check/all-poses.png`、元データは `docs/pose-check/src/`。

**でき上がり**: FBX は実行時に読まない。`models/poses.json`（6 KB）に関節のワールド位置
だけを持ち、向き合わせは `applyPose()` がアプリ側でやる。11 体で 1 ファイル共通、
体格差も吸収する。人物の設定パネルにサムネイル 12 枚（素の姿勢＋11）の一覧を出し、
押すとその場で着せ替わる。`it.posture` に ID が入り、共有リンクにも乗る。

```
node tools/make-poses.mjs   # docs/pose-check/src/*.fbx -> models/poses.json + サムネイル
```

ポーズを足すときは FBX を `docs/pose-check/src/` に置き、`tools/make-poses.mjs` の
`NAMES` と `ORDER` に 1 行足して走らせる。

**縮尺は「立ったときの身長」から出す。** 座った姿勢の外形から出すと、座った人が
身長ぶんの高さまで引き伸ばされて別人の大きさになる。

**`Box3.setFromObject` はスキニングを見ない。** ジオメトリの箱を行列で移すだけなので、
骨を回しても外形が変わらない。座らせても身長のままに見えて 1 度ハマった。
`posedBox()`（`SkinnedMesh.computeBoundingBox()` を使う）を通すこと。

**残っている癖**: 「椅子」のポーズは元データの座面が高く、身長 1.7 m で腰が 0.60 m に
来る。アプリの椅子の既定座面は 0.42 m なので、椅子の上に置くと浮く。寺村さんの指示は
「身長基準」なので現状はそのままにしてある。合わせるなら、腰の高さを身長から出して
そこに置く（最下点を床に落とすのをやめる）形になる。

## テスト

```
cd test && npm install && node run.mjs
```

`test/out/` にスクリーンショットが出る。確認しているのは、JS エラーの有無、
共有リンクの往復と復元、旧リンクの移行、GLB 取り込みの寸法、センサー換算値、
回転リングのドラッグ、閲覧モード、数値の直接入力、寸法ラベルが重ならないこと、
PDF の用紙サイズとページ数、用紙の再構図とファインダーが動かないこと、
保存ダイアログの 3 経路（OS のダイアログ／閉じたら何もしない／自前の名前入力）、
プロジェクトファイルの読み込み、人物モデルが身長どおりに縮尺されること、
身長を縮めても種別とモデルが変わらないこと、+ の一覧に全モデルがサムネイル付きで
並び、選んだ人がその身長で置かれること、ポーズ 12 種が並んで座る・寝るが効くこと。
テストが中を測れるように `window.__sp` を出してある（アプリ側は誰も読まない）。
`node run.mjs` は失敗があると終了コード 1 で落ちる。

## 現状

**動いていてヘッドレスで確認済み**: スタジオ寸法とホリゾント、4 つのビュー、
マネキン 4 種、小道具 4 種、カメラのフォーマット選択と手動幅の名前付き保存、
壁でクロップされる立体の撮影範囲、側面ビューの手前の壁の自動非表示、
パースでの壁の半透明化、目のマークでの表示切替、GLB の取り込みと寸法算出、
共有リンクと閲覧モード、PNG 書き出し、複数カメラと表示中カメラの切替、
寸法のリボン描画と項目ごとの寸法スイッチ、A4 の PDF 出力（横・縦とも 1 ページ、
A4 実寸であることを Chromium の PDF 生成で確認済み）、用紙の中での再構図、
プロジェクトファイルの保存（名前を付けて保存）と読み込み、固定サイズのマニピュレータ、
人物モデル 11 体（アジア系・アフリカ系・欧米系 × スーツ・カジュアル × 男女）と
サムネイルでの選択、起動時のパースビュー、ポーズ 11 種の適用。

**実装済み・実機確認待ち**: スマホとタブレットでの操作感全般、
**FBX の取り込み**（有効な FBX サンプルが用意できずヘッドレスで試せていない。
FBXLoader のモジュール解決までは確認済み）。

**設計判断**: センサーは機種ではなくフォーマットで持つ。取り込んだ 3D モデルの実体は
共有しない。人物モデルは寺村さん本人が権利を持つものだけ同梱する（配布ライセンスのため
拾い物は入れない）。人物モデルは外部ファイル。マネキンは 3 ポーズのまま。

**残り**: 子どものモデル（今は男の子・女の子はマネキンのみ）。欧米系の女性スーツも
1 体だけ欠けている。座面高の突き合わせ（上記）。3D スキャン背景の取り込み、
タイムコードや香盤との連携。
