# Studio Planner — 作業のためのメモ

スタジオ撮影の準備と打合せのための 3D Web アプリ。`index.html` 一枚で完結する。
開発者は寺村太一さん。日本語でやり取りする。

ローカルの作業フォルダは `D:\05_Claude\Studio-Planner`。

## いちばん大事なこと

**実機で確認していないものを「できました」と書かない。** ヘッドレスブラウザで通ったことと、
iPhone や iPad で本当に指で操作できることは別物。「ここまでは測れた／ここからは未確認」と
分けて書く。

## 誰のための道具か

CG スタッフではなく、**PC に不慣れなプロダクションマネージャー、カメラマン、照明チーム、
ディレクター**が現場で 30 秒で答えを得るための道具。この方針で機能を絞ってある。

- 欲しいのは 3D データではなく「一枚の絵」と「一枚の図面」。
- 光量や配光の物理計算はしない。ライトは位置と向きの記号だけ。
- アカウントとログインは作らない。登録の壁が最大の離脱要因になる。
- 機能を足すときは「初見のスタッフが迷わないか」で判断する。
- 置くものは ＋ ボタンのポップアップで選ぶ。パネルは今あるものを見る場所にする。
- パネルはリスト 1 本。スタジオもリストの 1 行で、選んだものの設定はリストの下に出る。
  タブを増やして往復させない。開閉は歯車ひとつ。

## やってはいけないこと

1. **機種名のリストを持たない。** カメラは毎年増える。センサーは「フォーマット」で持つ。
   規格は数十年変わらないので更新が要らない。同じ理由で、増え続けるものを
   ハードコードしそうになったら一度止まって考える。
2. **`file://` で開いて動作確認しない。** importmap と ES モジュールを使っているので
   必ず HTTP で配信する。`python -m http.server 8000` でよい。
3. **共有リンクに大きいデータを載せない。** URL ハッシュに圧縮 JSON を入れる方式なので、
   3D モデルの実体は絶対に入らない。実体は IndexedDB、リンクには寸法だけ。
4. **描画パスで一時的に隠した物を、当たり判定でそのまま使わない。**
   カメラ小窓の描画でマニピュレータを非表示にしたまま次のタッチ判定に入り、
   リングが永久に掴めなくなったことがある。判定は状態から引く。
5. **スライダー操作で全アイテムを作り直さない。** `rebuildItem()` で 1 個だけ差し替える。
   取り込んだモデルがあると全体再構築は目に見えて重い。
6. **取り込みの失敗を「読めませんでした」で終わらせない。** テクスチャが出ないときは
   だいたい原因が決まっている（別ファイルの画像、圧縮形式、メタリック既定値）。
   足りないファイル名を出す。ローダーが要求した URL は `LoadingManager` の
   `setURLModifier` で拾えるので、そこで記録する。
7. **環境の制約を推測で回避しない。実測する。** 「たぶん CSP だろう」で回避策を書くと、
   外れたときに原因が二重になる。1x1 の PNG を data URI・blob URL・fetch・
   createImageBitmap の 4 通りで試せば、どれが塞がれているかは数行で分かる。
   実際、Artifact のサンドボックスで塞がれていたのは `fetch(blob:)` だけだった。

## ファイル構成

```
index.html      アプリ本体。これ一枚で動く
README.md       使い方と公開方法
test/run.mjs    ヘッドレスブラウザでの確認スクリプト
```

## テスト

```
cd test && npm install && node run.mjs
```

`test/out/` にスクリーンショットが出る。確認しているのは、JS エラーの有無、
共有リンクの往復と復元、旧リンクの移行、GLB 取り込みの寸法、センサー換算値、
回転リングのドラッグ、パネルの開閉、閲覧モード、
チルトしたカメラが自分の絵に写り込まないこと（写り込みは、カメラを手で隠した絵と
1 ピクセルずつ比べて確かめる。そのあと選び直せることも確認する）、
カメラを増やしたときの名前・表示中の切り替え・リンクでの復元・削除時の引き継ぎ、
Delete キーでの削除と入力欄にいるときの誤爆防止、カメラ小窓の開閉、定規での寸法の出し入れ、
テクスチャ（GLB 埋め込み／.gltf + .bin + 画像／画像が足りないとき／ドロップ経路／
マテリアル未割り当て／参照なし／復号できない画像／`fetch(blob:)` を塞いだ環境）。

## 現状

**動いていてヘッドレスで確認済み**: スタジオ寸法とホリゾント、4 つのビュー、
マネキン 4 種、小道具 4 種、＋ ボタンのポップアップからの配置、
リストからのスタジオ編集、歯車でのパネル開閉、カメラ小窓の開閉、
定規ボタンでの寸法表示の切り替え、Delete キーでの削除、
カメラの複数台配置と表示するカメラの切り替え、
カメラのフォーマット選択と手動幅の名前付き保存、
壁でクロップされる立体の撮影範囲、側面ビューの手前の壁の自動非表示、
パースでの壁の半透明化、目のマークでの表示切替、GLB / glTF の取り込みと寸法算出、
取り込んだモデルのテクスチャ表示（`fetch(blob:)` が塞がれた環境での代替経路も含む）、
テクスチャが出ないときの原因の切り分け、共有リンクと閲覧モード、PNG 書き出し。

**実装済み・実機確認待ち**: スマホとタブレットでの操作感全般、
**FBX の取り込み**（有効な FBX サンプルが用意できずヘッドレスで試せていない。
FBXLoader のモジュール解決までは確認済み）、
**フォルダごとのドラッグ&ドロップ**（`webkitGetAsEntry` を使うので、合成した
DataTransfer では試せない。ファイルを直接ドロップする経路は確認済み）、
**DRACO / KTX2 / meshopt の圧縮 glTF**（デコーダを CDN から取りに行く経路までは
書いたが、実データで通していない）。

**設計判断**: センサーは機種ではなくフォーマットで持つ。3D モデルの実体は共有しない。
実写品質の人物モデルは同梱しない（配布ライセンスの問題）。CC0 のものを
寺村さんが用意した場合に組み込む。取り込んだモデルには環境マップを当てるが、
スタジオ側には当てない（図面の見た目を変えないため）。

**残り**: FBX の実機確認、圧縮 glTF の実データ確認、
反射面の「映り込み範囲」の可視化（調査メモで差別化点と結論づけた機能。まだ無い）、
CC0 人物モデルのプリセット化、実データも共有したい場合の保存先の検討、
タイムコードや香盤との連携。
